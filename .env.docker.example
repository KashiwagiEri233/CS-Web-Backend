# ============ CS-Web-Backend Docker 部署模板 ============
# 复制为 .env 并替换全部 CHANGE_ME / GENERATE 值：
#   cp .env.docker.example .env
# 密钥生成：
#   python -c "import secrets; print(secrets.token_urlsafe(32))"

# ---- 数据库（docker-compose 内 db 服务）----
# PostgreSQL 主机（docker-compose 内为服务名 db）
DATABASE_HOST=db
# PostgreSQL 端口
DATABASE_PORT=5432
# 数据库名
DATABASE_NAME=domefff
# 数据库用户
DATABASE_USER=postgres
# 必填：PostgreSQL 密码（同时用于 db 服务初始化）
DATABASE_PASSWORD=CHANGE_ME_DATABASE_PASSWORD

# ---- 必填密钥 ----
# JWT 签名密钥（≥32 字节，随机生成）
SECRET_KEY=GENERATE_SECURE_KEY
# TOTP 2FA secret 加密密钥（≥32 字节，随机生成）
TOTP_ENCRYPTION_KEY=GENERATE_TOTP_KEY

# ---- 前端 BFF / CORS ----
# 前端访问地址白名单（逗号分隔）。默认 http://localhost:2333（前端默认端口）
ALLOWED_ORIGINS=http://localhost:2333

# ---- 可选 ----
# 管理员重置用户密码时使用的默认密码
PASSWORD_RESET_DEFAULT=CHANGE_ME_RESET_PASSWORD
# 社区浏览去重 IP 哈希密钥（随机字符串；不设置时用内置兜底）
# COMMUNITY_IP_HASH_SECRET=
# GitHub OAuth（可选）
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=
# GITHUB_CALLBACK_URL=
# Redis（可选，不配置自动降级内存实现）
# REDIS_URL=redis://:password@redis:6379/0
# REQUIRE_REDIS_FOR_SECURITY=false
