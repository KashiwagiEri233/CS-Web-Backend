# 完整开发/CI 工具链。运行时部署只安装 requirements.txt。
-r requirements.txt
-r requirements-queue.txt

# 测试与覆盖率
pytest==7.4.3
httpx==0.25.2
httpx2==2.5.0
pytest-asyncio==0.21.1
pytest-cov>=5.0,<8.0

# 格式、静态检查与类型检查（统一使用 mypy，不再并行维护 pyright）
black==24.3.0
flake8==7.0.0
mypy==1.8.0

# 依赖锁定与供应链检查
pip-tools>=7.4,<8.0
pip-audit>=2.7,<3.0
cyclonedx-bom>=4.0,<8.0
pip-licenses>=4.0,<6.0
